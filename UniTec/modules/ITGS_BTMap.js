BTMap = {};
BTMap.context = {
	sql : null
};

// select request to find cross references
// makes a select request on the itemCrossReference table, with details from the Item table
// returns list of items
BTMap.findAndPlaceBT = function() {
  
}